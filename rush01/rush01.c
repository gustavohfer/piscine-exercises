#include <stdio.h>
#include <stdlib.h>

/*
1.1
    1:
        R1L = 4
        C1U = 4
    R1L = 3 ? 1, 2, 3



*/

char parse_input(char *str)
{
    int x; //horizontal
    int y; //vertical
    while (y <= 4)
    {
        while (x <= 4)
        {
                        
        }
    }
}

int main(int argc, char **argv)
{
    parse_input(argv[1]);
    printf("%s", argv[1]);
}